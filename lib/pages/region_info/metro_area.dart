import 'package:flutter/material.dart';

class MetroArea extends StatelessWidget {
  const MetroArea({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
